# ── Web Framework & API ───────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ── Database (SQLite dev / Postgres prod) ─────────────────────────────────────
sqlalchemy>=2.0.28
alembic>=1.13.1
asyncpg>=0.29.0
aiosqlite>=0.20.0
sqlmodel>=0.0.16

# ── Redis (event stream & caching) ───────────────────────────────────────────
redis>=5.0.3

# ── Agent Orchestration ───────────────────────────────────────────────────────
langgraph>=0.0.26
langchain>=0.1.12
langchain-community>=0.0.27

# ── LLM: Groq (fast inference, free tier available) ──────────────────────────
# https://console.groq.com — set GROQ_API_KEY in .env
langchain-groq>=0.1.0
groq>=0.37.1

# ── Embeddings: HuggingFace sentence-transformers (local, no API key) ─────────
# Default model: sentence-transformers/all-MiniLM-L6-v2 (384-dim, ~90 MB)
# Downloaded from HuggingFace Hub on first run, then cached locally.
langchain-huggingface>=0.0.3
sentence-transformers>=2.7.0
# tokenizers / transformers pulled in transitively by sentence-transformers

# ── Vector DB: Supabase pgvector ──────────────────────────────────────────────
# https://supabase.com — set SUPABASE_URL + SUPABASE_KEY in .env
supabase>=2.0.0

# ── Authentication & Security ─────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.1.2

# ── Configuration & Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.6.3
pydantic-settings>=2.2.1
httpx>=0.27.0
numpy>=1.26.0

# ── Development & Testing ─────────────────────────────────────────────────────
pytest>=8.1.1
pytest-asyncio>=0.23.5
black>=24.2.0
isort>=5.13.2
